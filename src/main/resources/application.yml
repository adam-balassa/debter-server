server:
  port: 8080
spring:
  application:
    name: Debter Server
info:
  version: '@version@'

---
spring:
  profiles:
    active: develop
amazon:
  dynamodb:
    endpoint: http://localhost:8081/
    tableName: debter
  aws:
    accesskey: key
    secretkey: key2

---
spring:
  profiles:
    active: production